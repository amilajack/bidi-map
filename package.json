{
  "name": "@amilajack/bidi-map",
  "version": "0.0.1",
  "description": "A bi-directional map",
  "main": "dist/index.js",
  "module": "dist/esm/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc && tsc --module esnext --lib esnext --outDir dist/esm",
    "test": "vitest"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@tsconfig/node18": "^1.0.0",
    "@types/node": "^17.0.36",
    "typescript": "^4.7.2",
    "vitest": "^0.13.0"
  }
}
